<%= form_for(booking_week) do |f| %>
  <% if booking_week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booking_week.errors.count, "error") %> prohibited this booking_week from being saved:</h2>

      <ul>
      <% booking_week.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label for="week">สัปดาห์ที่</label>
    <%= f.text_field :week %>
  </div>

  <div class="field">
    <label for="title">ระหว่างวันที่</label>
    <%= f.text_field :title %>
  </div>

  <div class="actions">

    <%= link_to 'Back', booking_weeks_path , class: 'btn btn-grey' %>
    <span class="mar-10"></span>

    <% if controller.action_name == 'edit' %>
      <%= link_to 'Show', @booking_week ,class: 'btn-grey'%>
      <span class="mar-10"></span>
    <% end %>

    <input type="submit" value="Submit" class="btn btn-redder">
  </div>
<% end %>
